# Generated by generate_stages.py, do not edit by hand
# Run 'make stages' to regenerate this file

name: Create and publish a Docker image

on:
  push:
    branches:
      - main
      - dev
  workflow_dispatch: null
  schedule:
    - cron: 0 0 * * *

env:
  REGISTRY: ghcr.io
  REPO_NAME: ${{ github.repository }}

jobs:
  r-debug:
    uses: cynkra/r-debug/.github/workflows/publish.yml@main
    with:
      path: r-debug
      image_name: r-debug
      architectures: '["amd64", "arm64"]'
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

  csan-r-debug:
    needs:
      - r-debug
    uses: cynkra/r-debug/.github/workflows/publish.yml@main
    with:
      path: r-debug/csan
      image_name: r-debug-csan
      architectures: '["amd64", "arm64"]'
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

  igraph-csan-r-debug:
    needs:
      - csan-r-debug
    uses: cynkra/r-debug/.github/workflows/publish.yml@main
    with:
      path: r-debug/csan/igraph
      image_name: r-debug-csan-igraph
      architectures: '["amd64", "arm64"]'
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

  duckdb-r-debug:
    needs:
      - r-debug
    uses: cynkra/r-debug/.github/workflows/publish.yml@main
    with:
      path: r-debug/duckdb
      image_name: r-debug-duckdb
      architectures: '["amd64", "arm64"]'
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

  san-r-debug:
    needs:
      - r-debug
    uses: cynkra/r-debug/.github/workflows/publish.yml@main
    with:
      path: r-debug/san
      image_name: r-debug-san
      architectures: '["amd64", "arm64"]'
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

  strictbarrier-r-debug:
    needs:
      - r-debug
    uses: cynkra/r-debug/.github/workflows/publish.yml@main
    with:
      path: r-debug/strictbarrier
      image_name: r-debug-strictbarrier
      architectures: '["amd64", "arm64"]'
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

  igraph-strictbarrier-r-debug:
    needs:
      - strictbarrier-r-debug
    uses: cynkra/r-debug/.github/workflows/publish.yml@main
    with:
      path: r-debug/strictbarrier/igraph
      image_name: r-debug-strictbarrier-igraph
      architectures: '["amd64", "arm64"]'
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

  threadcheck-r-debug:
    needs:
      - r-debug
    uses: cynkra/r-debug/.github/workflows/publish.yml@main
    with:
      path: r-debug/threadcheck
      image_name: r-debug-threadcheck
      architectures: '["amd64", "arm64"]'
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

  igraph-threadcheck-r-debug:
    needs:
      - threadcheck-r-debug
    uses: cynkra/r-debug/.github/workflows/publish.yml@main
    with:
      path: r-debug/threadcheck/igraph
      image_name: r-debug-threadcheck-igraph
      architectures: '["amd64", "arm64"]'
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

  valgrind-r-debug:
    needs:
      - r-debug
    uses: cynkra/r-debug/.github/workflows/publish.yml@main
    with:
      path: r-debug/valgrind
      image_name: r-debug-valgrind
      architectures: '["amd64", "arm64"]'
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

  igraph-valgrind-r-debug:
    needs:
      - valgrind-r-debug
    uses: cynkra/r-debug/.github/workflows/publish.yml@main
    with:
      path: r-debug/valgrind/igraph
      image_name: r-debug-valgrind-igraph
      architectures: '["amd64", "arm64"]'
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
